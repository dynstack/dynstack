#include <optional>

namespace DynStacking {
	namespace RollingMill {
		std::optional<std::string> calculate_answer(void* world_data, size_t len);
		//std::optional<DataModel::PlannedCraneMoves> plan_moves(DataModel::World& world);
	}
}